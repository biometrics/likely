Function Calls
--------------
**[Source Code](script/function_calls.likely)**

Likely uses a conventional function call syntax where arguments are provided in a canonical order:

    show(new, "new")
    n = new(f32,  1,        512,     512)
    --      type  channels  columns  rows

Any parameter in a function may have a default value bound to it. Arguments are assigned left-to-right first to unbound parameters then to bound parameters.

[Previous](index.html?show=comments) | [Next](index.html?show=closures)
