Currying
--------
**[Source Code](script/currying.likely)**

```
m = read("../data/misc/lenna.tiff")
m.saturation = true
```

Likely supports currying to compose higher order functions

```
fma = multiply{2} .. add{3}
n = fma(m)
show(n)
```

[Previous](./index.html?show=arithmetic)
