Closures
--------
The braces {} syntax returns a new function with the bound arguments:

    m = new{rows=512, columns=512, channels=1, type=i8}
    n = new{i8, 1, 512, 512} -- equivalent to m

    show(new)
    show(n)
    uninitialized_memory = n()

[Previous](?show=function_calls) | [Next](?show=arithmetic)
