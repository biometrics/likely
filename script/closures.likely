Closures
--------
**[Source Code](script/closures.likely)**

    m = new{rows=512, columns=512, type=i8}()

The braces {} syntax returns a new function with the bound arguments.

    setZero = set{0}
    show(set)
    show(setZero)

The function is just-in-time compiled using the bound arguments before execution.

    n = setZero(m)
    show(n)

set{0}(m)
    ^  ^
    |  Run-time arguments
    Compile-time arguments

[Previous](./index.html?show=function_calls) | [Next](./index.html?show=arithmetic)
