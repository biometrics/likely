[Currying](examples/currying.likely)
====================================

```
m = read("../data/misc/lenna.tiff")
m.saturation = true
```

Likely supports currying to compose higher order functions\dream{activate:n}

```
fma = multiply{2} .. add{3}
n = fma(m)
```

[Previous](./index.html?show=examples/arithmetic.likely) | [Next](./index.html?show=examples/mandelbrot_set.likely)
