Closures
--------
The braces **{ }** syntax returns a new function with bound arguments:

    my_new = new{u8, 1, 512, 512}

    -- Equivalent to my_new
    my_new_alt = new{rows=512, columns=512, type=u8}

    show(new, "new")
    show(my_new, "my_new")

    -- Equivalent to calling new(u8, 1, 512, 512)
    uninitialized_matrix = my_new()

[Previous](?show=function_calls) | [Next](?show=arithmetic)
