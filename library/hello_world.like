Hello World
-----------
**CTRL+scroll** to edit numerical constants, note that it is re-executed as you edit:

    (define hello_world
      (kernel (a)
        (/ a (cast 2 (type a)))))

    (hello_world (read "../data/misc/lenna.tiff"))

[Next](?show=comments)
